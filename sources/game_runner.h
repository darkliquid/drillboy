// require core include to pull in basics
#include <ClanLib/core.h>

class GameRunner {
	public:
		// public static method, provides program arguments
		// as a vector of clanlib strings
		static int main(const std::vector<CL_String> &args);
};
